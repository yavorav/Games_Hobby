# Defined functions for the game

import data
import char_player
# imports include player class, monster class, weapon class

def attack(attacker, defender):
    # define class of attacker and defender? which is the player vs monster
    # then we have access to attacker.hit()
    # does it return anything? defender health?
    # or do the checks in the function if someone died and call the outcome here?
    # can't handle player death in this function... needs to break entire game function to kill player
    
    pass